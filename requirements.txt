# requirements.txt

# Core FastAPI + server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Database (PostgreSQL + ORM)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# Environment variables
python-dotenv>=1.0.0

# Authentication & security
passlib[bcrypt]>=1.7.0
python-jose[cryptography]>=3.3.0
pydantic[email]>=2.0.0

# File upload support (needed for images to Gemini)
python-multipart>=0.0.9

# Optional but very useful
pydantic-settings>=2.0.0          # For better config handling
email-validator>=2.0.0            # For email validation in registration